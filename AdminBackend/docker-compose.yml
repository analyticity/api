version: "3.9"

services:
  admin-backend:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./Admin-Backend:/app/Admin-Backend
    container_name: Admin-Backend
    ports:
      - "8020:8020"
    networks:
      - analyticity-network

networks:
  analyticity-network:
    external: true