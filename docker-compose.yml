version: '3.8'

services:
  api:
    build: .
    image: ghcr.io/analyticity/api:latest
    container_name: analyticity-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped