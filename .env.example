# Database Configuration
POSTGRES_HOST_BRNO=localhost
POSTGRES_PORT_BRNO=5432
POSTGRES_DB_BRNO=analyticity
POSTGRES_USER_BRNO=postgres
POSTGRES_PASSWORD_BRNO=your_password_here

# For Docker Compose (uncomment and use these instead):
# POSTGRES_HOST_BRNO=db
# POSTGRES_PORT_BRNO=5432

# Leave empty or remove these variables to use fallback mode with example data

